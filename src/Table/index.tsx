import React from 'react';

interface TableProps {
  columns: any[],
  dataSource: any[],
  pagination: object,
  rowKey: string,
};

const Table: React.FunctionComponent = (props) => {
  return <div>

  </div>
}